[project]
name = "tuxoff"
version = "0.1.0"
description = "easy parser for cracked games"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.58.0",
    "playwright-stealth>=2.0.2",
    "setuptools>=82.0.1",
]

[project.scripts]
tuxoff = "tuxoff.main:run"
